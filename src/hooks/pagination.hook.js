import React from 'react';

export const usePagination = () => {
    const [pages, setPages] = useState({
        prevPage: null,
        currentPage: 1,
        nextPage: null,
        totalPages: null,
        pageSize: null
    });

    const getPagesInfo = (pageInfo) => {

    }

    return {};
}